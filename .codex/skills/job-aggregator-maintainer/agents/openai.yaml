interface:
  display_name: "Job Aggregator Maintainer"
  short_description: "Maintain the Job Aggregator core, crawler, and candidate services"
  default_prompt: "Use $job-aggregator-maintainer to work on the job-aggregator core API, crawler API, candidate services, Docker images, and deployment topology."
